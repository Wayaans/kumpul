# handoff

Transfer conversation context into a new focused pi session.

## Commands

- **/handoff** `<goal>` — summarize the current branch, generate a handoff prompt with the active model, open it in the editor for review, then create a linked child session prefilled with the result.

## Install

No extension-local dependencies.
