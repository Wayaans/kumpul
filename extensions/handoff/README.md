# handoff

Transfer conversation context into a new focused pi session.

## Commands

- **/handoff** `<goal>` — summarize the current branch, generate a handoff prompt with suggested skills, redaction guidance, and the current `## Context` / `## Task` format, then open it in the editor for review and create a linked child session prefilled with the result.

## Install

No extension-local dependencies.
